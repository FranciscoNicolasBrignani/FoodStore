export interface Productos {
    id: string;
    nombre: string;
    descripcion: string;
    precio: number;
    imagen: string;
    categoria: string
}

export interface Categorias {
    id: string;
    nombre: string;
}